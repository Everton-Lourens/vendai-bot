/* eslint-disable no-unused-vars */
export enum STATUS_MESSAGE {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
  FAILED = 'failed',
  CANCELED = 'canceled',
  APPROVED = 'approved',
}
